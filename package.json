{
  "name": "project-design-pattern-factory",
  "version": "1.0.0",
  "description": "Factory Pattern Pock",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.0.5",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.0",
    "vitest": "^3.0.5"
  }
}
